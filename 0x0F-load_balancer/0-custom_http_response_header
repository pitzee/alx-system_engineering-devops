#!/bin/bash
# Install Nginx
sudo apt-get update
sudo apt-get install -y nginx

# Add custom header configuration
sudo bash -c "echo 'add_header X-Served-By \$hostname;' > /etc/nginx/conf.d/custom-header.conf"

# Restart Nginx to apply changes
sudo service nginx restart
